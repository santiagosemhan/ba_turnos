{% extends 'admin_base.html.twig' %}

{% set __title = 'Cambiar Password' %}

{% block content %}
    <div class="box box-success">
        <div class="box-header with-border">
            <h3 class="box-title">Usuario: {{ app.user }}</h3>
        </div>
        <div class="box-body">
            <!-- form start -->
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="box-footer">
                <input class="btn btn-success" type="submit" value="Cambiar Password"/>
                <a class="btn btn-default" href="{{ path('admin_homepage') }}"> Volver</a>
            </div>

            {{ form_end(form) }}
        </div>
        <!-- /.box-body -->
    </div>
    <!-- /.box -->


{% endblock %}